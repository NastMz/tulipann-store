/**
 * Department interface.
 *
 * @interface Department
 * @property {string} id - Department id.
 * @property {string} name - Department featureName.
 */
export interface Department {
    id: string;
    name: string;
}