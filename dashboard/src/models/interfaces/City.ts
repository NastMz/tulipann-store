/**
 * City interface.
 *
 * @interface City
 * @property {string} id - City id.
 * @property {string} name - City featureName.
 * @property {string} departmentId - Department id.
 */
export interface City {
    id: string;
    name: string;
    departmentId: string;
}